#include<stdio.h>
void main()
{
int num;
printf(“enter the elements”);
scanf(“%d”,&num);
if(num>0)
printf(“%d is a positive num”);
else if(num<0)
printf(“%d is a negative num”);
else if
printf(“%d is a zero”);
return 0;
}
}
